project_id = "mon-projet-gcp"
region     = "europe-west1"
image      = "europe-west1-docker.pkg.dev/mon-projet-gcp/apps/scaling-demo:latest"

# Config de scaling (modifier puis `terraform apply` pour ajuster)
min_instances = 0
max_instances = 10
concurrency   = 80
cpu           = "1"
memory        = "512Mi"
